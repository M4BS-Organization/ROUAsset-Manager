# 完全性検証チェックリスト — Issue #26 fc_系仕訳出力フォーム共通基盤

作成日: 2026-03-19
対象: PR #85 (fae5741) の実装内容 vs Access版仕様

---

## 凡例

- ○ = 完全達成
- △ = 部分達成（機能するが不完全）
- × = 未達成（機能しない）
- 未 = 未着手（コードなし）

---

## カテゴリ1: UI完全性（UIの再現）

### 1-A. 支払仕訳フォーム（Form_fc_支払仕訳_KITOKU）

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 1-1 | txt_SLIP_DT（伝票日付）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-2 | txt_SLIP_NO_START_VAL（伝票番号開始値）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-3 | txt_OUTPUT_FOLDER_NM（出力先フォルダ）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-4 | txt_KAMOKU_CD（科目コード）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-5 | txt_KAMOKU_NM（科目名称）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-6 | txt_BSHO_CD テキストボックスが存在する | △ | 存在するがラベルが「部署ｺｰﾄﾞ(一括)」。Access版は「補助科目コード」であり意味が異なる（Gap A1） |
| 1-7 | txt_OUTPUT_FILE1_NM〜FILE4_NM（出力ファイル名×4）テキストボックスが存在する | ○ | Designer.vb に4つとも実装確認済み |
| 1-8 | cmd_実行ボタンが存在する | ○ | Designer.vb に実装確認済み（Text="実行(&R)"） |
| 1-9 | cmd_CANCELボタンが存在する | ○ | Designer.vb に実装確認済み（Text="ｷｬﾝｾﾙ(&C)"） |
| 1-10 | cmd_選択ボタン（フォルダ選択）が存在する | ○ | Designer.vb に実装確認済み |
| 1-11 | ラベル類（lbl_SLIP_DT, lbl_KAMOKU等 17個）が存在する | ○ | Designer.vb に17個のラベル確認済み |
| 1-12 | フォームタイトルが「支払仕訳出力画面」である | ○ | Designer.vb: Text="支払仕訳出力画面" |
| 1-13 | デフォルト値（KAMOKU_CD=4160, KAMOKU_NM=未払金）が設定される | ○ | LoadSettings で t_settei からデフォルト値を読み込み |

### 1-B. 計上仕訳フォーム（Form_fc_計上仕訳_KITOKU）

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 1-14 | txt_SLIP_DT（伝票日付）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-15 | txt_SLIP_NO_START_VAL（伝票番号開始値）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-16 | txt_OUTPUT_FOLDER_NM（出力先フォルダ）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-17 | txt_OUTPUT_FILE_NM（出力ファイル名）テキストボックスが存在する | △ | 1つのみ存在。Access版は4ファイル出力だが計上仕訳UIは1ファイル名のみ（Gap A2）。Access版でもCMSW2WRKが主出力のため許容範囲の可能性あり |
| 1-18 | cmd_実行 / cmd_CANCEL / cmd_選択 の3ボタンが存在する | ○ | Designer.vb に3つとも確認済み |
| 1-19 | KAMOKU_CD / BSHO_CD フィールドが存在しない（Access版と一致） | ○ | Access版の計上仕訳にも科目コード入力欄はなく、VB.NET版も同様 |
| 1-20 | フォームタイトルが「計上仕訳出力画面」である | ○ | Designer.vb: Text="計上仕訳出力画面" |

### 1-C. サブフォーム（Form_fc_支払仕訳_KITOKU_SUB）

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 1-21 | txt_ZRITU（消費税率）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-22 | txt_ZEI_CD（税処理コード）テキストボックスが存在する | ○ | Designer.vb に実装確認済み |
| 1-23 | ラベル（"消費税率"/"税処理ｺｰﾄﾞ"）が存在する | ○ | Designer.vb に2つのラベル確認済み |
| 1-24 | 親フォームからサブフォームを表示するUI要素が存在する | × | 親フォーム（Form_fc_支払仕訳_KITOKU）にSUBフォーム表示ボタン/トリガーが存在しない（Gap A3/G1） |

---

## カテゴリ2: 機能完全性（ビジネスロジックの再現）

### 2-A. 初期値ロード処理

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 2-1 | 支払仕訳: Form_Load で tw_s_keijo_joken 存在確認を行う | ○ | ValidateJokenAndGetKikanFrom() を Form_Load 内で呼び出し |
| 2-2 | 支払仕訳: Form_Load で設定値（10項目）を t_settei から読み込む | ○ | LoadSettings() で10項目読み込み実装済み |
| 2-3 | 計上仕訳: Form_Load で設定値（4項目）を t_settei から読み込む | ○ | LoadSettings() で4項目読み込み実装済み |
| 2-4 | 伝票番号開始値を NEXTVAL 方式で採番テーブルから取得する | × | Access版は NEXTVAL 方式だが、VB.NET版はデフォルト値 "1" を使用（Gap F2） |

### 2-B. 実行ボタンの処理フロー

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 2-5 | 支払仕訳: cmd_実行_Click でバリデーション→実行→設定保存の順に処理する | ○ | cmd_実行_Click 内で ValidateOutputFolder → ExecuteKitoku → SaveSettings の順で実装 |
| 2-6 | 計上仕訳: cmd_実行_Click でバリデーション→実行→設定保存の順に処理する | ○ | 同様のフロー実装済み |
| 2-7 | 実行確認ダイアログを表示する | ○ | ConfirmExecute() で MessageBox.Show 実装済み |

### 2-C. 仕訳データ作成ロジック

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 2-8 | 支払仕訳: m仕訳_売買現預金() 相当ロジックが実装されている | × | 未実装。BuildInsertToWrkSql で単一UNION ALL SQLのみ（Gap B1） |
| 2-9 | 支払仕訳: m仕訳_売買未払金() 相当ロジックが実装されている | × | 未実装（Gap B1） |
| 2-10 | 支払仕訳: m仕訳_賃貸消費税一括有現預金() 相当ロジックが実装されている | × | 未実装（Gap B1） |
| 2-11 | 支払仕訳: m仕訳_賃貸消費税一括有未払金() 相当ロジックが実装されている | × | 未実装（Gap B1） |
| 2-12 | 支払仕訳: m仕訳_賃貸消費税一括無現預金() 相当ロジックが実装されている | × | 未実装（Gap B1） |
| 2-13 | 支払仕訳: m仕訳_賃貸消費税一括無未払金() 相当ロジックが実装されている | × | 未実装（Gap B1） |
| 2-14 | 計上仕訳: m仕訳データ作成_開始計上() 相当ロジック（1D/1C/2D/2C）が実装されている | × | 未実装。BuildInsertToWrkSql で単一SQLのみ（Gap B2） |
| 2-15 | 計上仕訳: m仕訳データ作成_税一括() 相当ロジック（D/C）が実装されている | × | 未実装（Gap B2） |
| 2-16 | 計上仕訳: m仕訳データ作成_減価償却() 相当ロジックが実装されている | × | 未実装（Gap B2） |

### 2-D. データ前処理・バリデーション

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 2-17 | m仕訳作成元データCHK() 相当のデータ整合性チェックが実装されている | × | 資産科目マスタ・HREL結合・科目コード未設定チェックが全て欠如（Gap E1） |

### 2-E. その他機能

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 2-18 | 設定値の保存（SaveSettings）が実装されている | ○ | 支払仕訳10項目・計上仕訳4項目ともに FcSetteiHelper 経由で UPSERT 実装済み |
| 2-19 | 設定値の復元（LoadSettings）が実装されている | ○ | t_settei から GetText で読み込み実装済み |
| 2-20 | フォルダ選択ダイアログが実装されている | ○ | cmd_選択_Click で FolderBrowserDialog 使用 |
| 2-21 | キャンセル処理（フォーム閉じる）が実装されている | ○ | cmd_CANCEL_Click で Me.Close() 実装済み |
| 2-22 | サブフォーム（消費税率/税処理コード入力）の呼び出しが実装されている | × | 親フォームからの呼び出しコードが存在しない（Gap G1） |
| 2-23 | 科目コード分割ロジック（gDivKMK_CD 相当: "4160-001" → KMK_CD/HKM_CD）が実装されている | × | 共通関数未実装（Gap B4） |

---

## カテゴリ3: ファイル出力完全性

### 3-A. 支払仕訳の出力ファイル

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 3-1 | CMSW2WRK ファイル（File1）が出力される | ○ | WriteOutputFile で tw_kitoku_cmsw2wrk → FixedLengthFileWriter.WriteFile 実装済み |
| 3-2 | APGDHWRK ファイル（File2）が出力される | △ | WriteApgdhFile 実装済みだが、72列中3列のみ INSERT。大半が NULL/デフォルト（Gap C3） |
| 3-3 | APGDDWRK ファイル（File3）が出力される | △ | WriteApgddFile 実装済みだが、34列中6列のみ INSERT（Gap C3） |
| 3-4 | APGDSWRK ファイル（File4）が出力される | △ | WriteApgdsFile 実装済みだが、63列中4列のみ INSERT（Gap C3） |

### 3-B. 計上仕訳の出力ファイル

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 3-5 | CMSW2WRK ファイル（File1）が出力される | ○ | ExecuteKeijo で tw_kitoku_cmsw2wrk → WriteOutputFile 実装済み |
| 3-6 | APGDHWRK ファイル（File2）が出力される | × | 計上仕訳は CMSW2WRK 1ファイルのみ出力。APGD系未実装（Gap C1） |
| 3-7 | APGDDWRK ファイル（File3）が出力される | × | 同上（Gap C1） |
| 3-8 | APGDSWRK ファイル（File4）が出力される | × | 同上（Gap C1） |

### 3-C. 固定長フォーマット

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 3-9 | CMSW2WRK: 52フィールド定義が Access版 Lines 146-197 と一致する | ○ | GetCMSW2WRKFields() で52フィールド定義済み。レコード長571バイト |
| 3-10 | APGDHWRK: 72フィールド定義が Access版 Lines 266-346 と一致する | △ | GetAPGDHWRKFields() で72フィールド定義済みだが、ワークテーブルへの INSERT が3列のみのため出力データの大半が空白 |
| 3-11 | APGDDWRK: フィールド定義が Access版 Lines 417-451 と一致する | △ | 定義は34フィールドだがコメントは31と不一致（Gap C4）。INSERT は6列のみ |
| 3-12 | APGDSWRK: フィールド定義が Access版 Lines 519-581 と一致する | △ | 定義は63フィールドだがコメントは62と不一致（Gap C4）。INSERT は4列のみ |
| 3-13 | 固定長フォーマット: Shift-JIS エンコーディングで出力される | ○ | FixedLengthFileWriter: Encoding.GetEncoding("Shift_JIS") 使用 |
| 3-14 | 固定長フォーマット: バイト単位の右パディングが正しく動作する | ○ | PadRightByte で Shift-JIS バイト幅ベースのパディング実装済み |

### 3-D. ファイル拡張子

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 3-15 | 出力ファイル拡張子が Access版（.tmp）と一致する | × | VB.NET版は .txt で統一。Access版は .tmp（Gap C2） |

---

## カテゴリ4: DB処理完全性

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 4-1 | 支払仕訳: BuildInsertToWrkSql の列マッピングが Access版 m仕訳データ作成 と一致する | △ | TODO(L117)が残存。「Access版と照合して正確な列マッピングに更新する」未完了（Gap D2） |
| 4-2 | 計上仕訳: BuildInsertToWrkSql の列マッピングが Access版 m仕訳データ作成 と一致する | △ | TODO(L77)が残存。同上（Gap D2） |
| 4-3 | 支払仕訳: tw_kitoku_cmsw2wrk テーブルが52列（+id）で定義されている | ○ | sql/004_tw_kitoku_tables.sql で定義確認済み |
| 4-4 | 支払仕訳: tw_kitoku_apgdhwrk テーブルが72列（+id）で定義されている | ○ | sql/004_tw_kitoku_tables.sql で定義確認済み |
| 4-5 | 支払仕訳: tw_kitoku_apgddwrk テーブルが34列（+id）で定義されている | ○ | sql/004_tw_kitoku_tables.sql で定義確認済み |
| 4-6 | 支払仕訳: tw_kitoku_apgdswrk テーブルが63列（+id）で定義されている | ○ | sql/004_tw_kitoku_tables.sql で定義確認済み |
| 4-7 | SQLインジェクション対策としてパラメータ化クエリが使用されている | × | BuildInsertToWrkSql 内で文字列補間による直接埋め込み。NpgsqlParameter 未使用（Gap D3） |
| 4-8 | 支払仕訳: BuildApgdhWrk の INSERT 列が Access版 gAPGDHWRK作成 と一致する | × | 72列中3列のみ INSERT。TODO(L270)残存（Gap C3） |
| 4-9 | 支払仕訳: BuildApgddWrk の INSERT 列が Access版 gAPGDDWRK作成 と一致する | × | 34列中6列のみ INSERT。TODO(L279)残存（Gap C3） |
| 4-10 | 支払仕訳: BuildApgdsWrk の INSERT 列が Access版 gAPGDSWRK作成 と一致する | × | 63列中4列のみ INSERT。TODO(L287)残存（Gap C3） |

---

## カテゴリ5: エラー処理・バリデーション

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 5-1 | 出力先フォルダの存在チェックが実装されている | ○ | ValidateOutputFolder() でフォルダ存在チェック実装済み |
| 5-2 | 出力ファイルパスの詳細チェック（gOutputFile_Check 相当）が実装されている | △ | フォルダ存在のみ。ファイル名妥当性・上書き確認は未実装（Gap E2） |
| 5-3 | 資産科目マスタの費目決定要素CD未入力チェックが実装されている | × | m仕訳作成元データCHK 相当処理なし（Gap E1） |
| 5-4 | HREL（費目関連テーブル）との結合チェックが実装されている | × | 同上（Gap E1） |
| 5-5 | 科目コード未設定データの有無確認が実装されている | × | 同上（Gap E1） |
| 5-6 | 件数ゼロ時の警告メッセージが表示される | ○ | ExecuteKitoku/ExecuteKeijo 内で COUNT(*)==0 時に警告表示実装済み |
| 5-7 | サブフォーム txt_ZEI_CD の gLenCheck() バリデーションが実装されている | × | VB.NET版の SUB フォームにバリデーションなし（Gap E3） |

---

## カテゴリ6: 共通基盤完全性（Issue #26 本来の要件）

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 6-1 | FcJournalOutputBase が Template Method パターンで設計されている | ○ | MustInherit クラスに Execute() Template Method + MustOverride メンバー4つ |
| 6-2 | FcJournalOutputBase.Execute() の処理フロー（7ステップ）が正しく実装されている | ○ | ValidateJoken → ClearWork → BuildSQL → Execute → CountCheck → Select → WriteFile |
| 6-3 | KITOKU フォームが基底クラスの Execute() Template Method を使用している | × | ExecuteKitoku/ExecuteKeijo という独自フローを使用。基底の Execute() は未使用（Gap B5/H3） |
| 6-4 | FcSetteiHelper による設定の永続化（保存・復元）が動作する | ○ | t_settei テーブルへの UPSERT + GetText で動作。FcSetteiHelper 完成状態 |
| 6-5 | 設定駆動による出力形式切り替え（固定長/CSV/Excel）が実装されている | × | FcSetteiHelper は実装済みだが出力形式切り替えロジックは未実装（Gap F3） |
| 6-6 | pc_仕訳出力 共通クラス相当の機能が共通基盤に存在する | × | gDivKMK_CD, gOutputFile_Check, gAPGDxWRK出力 等の共通メソッドが未実装。KITOKUフォーム内にインライン実装（Gap B3） |
| 6-7 | 最低2顧客の固有仕訳出力が共通基盤上で動作する | × | KITOKU 1顧客のみ実装。かつ基底クラスの Execute() を使わない独自フロー（Gap H1/H3） |
| 6-8 | KITOKU以外の顧客（MYCOM, TSYSCOM等）への適用可能性が実証されている | × | 他顧客は全てスケルトンのみ。動作可能な実装なし（Gap H2） |
| 6-9 | FixedLengthFileWriter が汎用的に使えるモジュールとして実装されている | ○ | Public Shared メソッドで DataTable + フィールド定義を受け取る汎用設計。完成状態 |
| 6-10 | KitokuFixedLengthFormats がフォーマット定義として完成している | ○ | 4ワークテーブル分のフィールド定義（52+72+34+63）実装済み。TODO なし |

---

## カテゴリ7: 遷移フロー

| # | チェック項目 | 達成状況 | 判定根拠 |
|---|---|---|---|
| 7-1 | メインメニューから支払仕訳フォームへの遷移経路が存在する | △ | メインメニューの動的権限制御は Issue #27 で実装済みだが、fc_系フォームへの具体的な遷移ボタン/メニュー項目の確認が必要 |
| 7-2 | メインメニューから計上仕訳フォームへの遷移経路が存在する | △ | 同上 |
| 7-3 | 支払仕訳フォームからサブフォーム（消費税率入力）への遷移が動作する | × | 親フォームにサブフォーム表示トリガーが存在しない（Gap G1） |
| 7-4 | サブフォームの値（消費税率・税処理コード）が親フォームに反映される | × | SUB フォーム自体は Zritu/ZeiCd プロパティを持つが、親フォームからの呼び出しが未実装のため反映不可 |
| 7-5 | フォーム閉じる時に CrudHelper / FcSetteiHelper が正しく Dispose される | ○ | FormClosed イベントで _settei?.Dispose() 実装済み。基底クラスの Dispose で _crud も破棄 |

---

## サマリー

### 達成状況集計

| 達成状況 | 件数 | 割合 |
|---|---|---|
| ○（完全達成） | 30 | 49.2% |
| △（部分達成） | 9 | 14.8% |
| ×（未達成） | 22 | 36.1% |
| 未（未着手） | 0 | 0.0% |
| **合計** | **61** | **100%** |

### カテゴリ別達成率

| カテゴリ | 項目数 | ○ | △ | × | 達成率(○のみ) |
|---|---|---|---|---|---|
| 1. UI完全性 | 24 | 20 | 3 | 1 | 83.3% |
| 2. 機能完全性 | 23 | 10 | 0 | 13 | 43.5% |
| 3. ファイル出力完全性 | 15 | 5 | 5 | 5 | 33.3% |
| 4. DB処理完全性 | 10 | 4 | 2 | 4 | 40.0% |
| 5. エラー処理・バリデーション | 7 | 2 | 1 | 4 | 28.6% |
| 6. 共通基盤完全性 | 10 | 4 | 0 | 6 | 40.0% |
| 7. 遷移フロー | 5 | 1 | 2 | 2 | 20.0% |

### 最重要未達成項目（致命的/高深刻度）

1. **2-8〜2-13（×）**: 支払仕訳の6種仕訳サブ関数が全て未実装（Gap B1: 致命的）
2. **2-14〜2-16（×）**: 計上仕訳の3種データ作成関数が全て未実装（Gap B2: 致命的）
3. **4-1, 4-2（△）**: SQL列マッピングが TODO 状態で未検証（Gap D2: 高）
4. **3-6〜3-8（×）**: 計上仕訳の APGD系3ファイルが未出力（Gap C1: 高）
5. **4-8〜4-10（×）**: APGD系ワークテーブルの列充足率が極端に低い（Gap C3: 高）
6. **1-24, 2-22, 7-3（×）**: サブフォーム呼び出し未実装（Gap G1: 高）
7. **2-17, 5-3〜5-5（×）**: データ前処理チェック欠如（Gap E1: 高）
8. **6-7（×）**: 2顧客動作の完了条件未達（Gap H1: 高）
